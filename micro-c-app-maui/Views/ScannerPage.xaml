<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI"
             x:Class="micro_c_app_maui.Views.ScannerPage"
             x:Name="root">
    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="scanner"
                                        IsDetecting="True"
                                        HorizontalOptions="FillAndExpand"
                                        VerticalOptions="FillAndExpand"
                                        BarcodesDetected="OnBarcodesDetected" />
        <Button x:Name="torchButton"
                Text="Torch"
                HorizontalOptions="End"
                VerticalOptions="Start"
                Margin="20"
                Clicked="TorchClicked" />
        <!-- TEMPORARY diagnostic label - remove once scanning is confirmed working on-device. -->
        <Label x:Name="debugLabel"
               Text="waiting for frames..."
               TextColor="Lime"
               BackgroundColor="#AA000000"
               FontSize="12"
               Padding="8"
               HorizontalOptions="Fill"
               VerticalOptions="End" />
    </Grid>
</ContentPage>
